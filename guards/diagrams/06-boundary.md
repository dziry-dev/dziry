# The shared-memory boundary

> Generated by `bun run arch-diagram`. Do not edit — regenerate.

Read directly from `src/protocol/schema.ts`, so it cannot disagree with the protocol. Version 10.

```mermaid
flowchart LR
  writer["<b>App thread</b><br/>src/host/worker.ts"]
  reader["<b>Engine</b><br/>native-src/dziry-engine/src/tables.rs"]
  subgraph shared["Shared memory · protocol v10 · struct-of-arrays"]
    t_nodes["<b>nodes</b><br/>9 fields · 23 B/elem<br/><small>sized by nodes</small>"]
    t_styles["<b>styles</b><br/>58 fields · 177 B/elem<br/><small>sized by styles</small>"]
    t_variants["<b>variants</b><br/>3 fields · 12 B/elem<br/><small>sized by own</small>"]
    t_variantSlots["<b>variantSlots</b><br/>1 fields · 2 B/elem<br/><small>sized by own</small>"]
    t_media["<b>media</b><br/>3 fields · 9 B/elem<br/><small>sized by own</small>"]
    t_lists["<b>lists</b><br/>7 fields · 28 B/elem<br/><small>sized by own</small>"]
    t_layout["<b>layout</b><br/>4 fields · 16 B/elem<br/><small>sized by nodes</small>"]
    t_strings["<b>strings</b><br/>2 fields · 8 B/elem<br/><small>sized by strings</small>"]
  end

  writer --> t_nodes
  t_nodes --> reader
  writer --> t_styles
  t_styles --> reader
  writer --> t_variants
  t_variants --> reader
  writer --> t_variantSlots
  t_variantSlots --> reader
  writer --> t_media
  t_media --> reader
  writer --> t_lists
  t_lists --> reader
  writer --> t_layout
  t_layout --> reader
  writer --> t_strings
  t_strings --> reader
```

| Table | Fields | Bytes/elem | Sized by |
| --- | --- | --- | --- |
| `nodes` | 9 | 23 | nodes |
| `styles` | 58 | 177 | styles |
| `variants` | 3 | 12 | own |
| `variantSlots` | 1 | 2 | own |
| `media` | 3 | 9 | own |
| `lists` | 7 | 28 | own |
| `layout` | 4 | 16 | nodes |
| `strings` | 2 | 8 | strings |
