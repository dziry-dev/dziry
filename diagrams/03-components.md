# Components

> Generated by `bun run arch-diagram`. Do not edit — regenerate.

Inside the three containers with enough moving parts to be worth opening: the compiler, the app thread, and the engine.

## Compiler

```mermaid
C4Component
  title dziri — components (compiler)

  Container_Boundary(b_compiler, "Compiler") {
    Component(jsx, "JSX evaluation", "src/compiler/jsx-runtime.ts", "Runs the author's module and builds a real element tree — no virtual DOM, no diff.")
    Component(css, "CSS parser & cascade", "src/compiler/css.ts", "Selector matching, specificity, inheritance, shorthand expansion, unit resolution.")
    Component(variants, "Variants", "src/compiler/variant-compile.ts", "Hover, focus and the rest resolved ahead of time into patch tables, so interaction state costs one u16 rather than a selector match.")
    Component(reactive, "Reactive transform", "src/compiler/reactive-transform.ts", "Rewrites signal reads at build time — bare reads, no `.value`, no deps arrays.")
    Component(routes, "Routes & windows", "src/compiler/routes.ts", "Every route is compiled into the same node table; showing one writes a byte.")
    Component(emit, "Emitter", "src/compiler/compile.ts", "Interns styles and writes the typed arrays plus the runtime refs.")
  }

  Rel(jsx, css, "element tree to match against")
  Rel(css, variants, "matched rules, by state")
  Rel(reactive, jsx, "rewritten module")
  Rel(routes, jsx, "one tree per route")
  Rel(variants, emit, "patch tables")
  Rel(css, emit, "resolved styles")

  UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="2")
```

## App thread

```mermaid
C4Component
  title dziri — components (appThread)

  Container_Boundary(b_appThread, "App thread") {
    Component(signals, "Signals", "src/runtime/signal.ts", "The whole reactivity core. Bare reads; the plugin did the wiring at build time.")
    Component(bindings, "Text bindings", "src/runtime/bindings.ts", "A signal to a text span — the only thing that rewrites glyphs.")
    Component(patches, "Style patches", "src/runtime/patches.ts", "A signal to a style field, written straight into the interned table.")
    Component(lists, "List arenas", "src/runtime/list-runtime.ts", "Insert and remove by relinking siblings — nothing renumbers.")
    Component(upload, "Uploader", "src/engine/upload.ts", "Grows capacities and writes the IR into engine memory.")
  }

  Rel(signals, bindings, "notifies")
  Rel(signals, patches, "notifies")
  Rel(signals, lists, "notifies")
  Rel(bindings, upload, "dirty spans")
  Rel(patches, upload, "dirty spans")
  Rel(lists, upload, "relinked siblings")

  UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="2")
```

## Engine

```mermaid
C4Component
  title dziri — components (engine)

  Container_Boundary(b_engine, "Engine") {
    Component(rsEngine, "Engine core", "native-src/dziri-engine/src/engine.rs", "Handle registry, the tick, staged/live commit.")
    Component(rsLayout, "Layout", "native-src/dziri-engine/src/layout.rs", "Builds Taffy styles from the shared tables and runs the solve.")
    Component(rsPaint, "Paint", "native-src/dziri-engine/src/paint.rs", "Skia: boxes, borders, radii, shadows, clips.")
    Component(rsText, "Text", "native-src/dziri-engine/src/text.rs", "SkParagraph: shaping, measurement, wrapping.")
    Component(rsWindow, "Window & input", "native-src/dziri-engine/src/window.rs", "SDL3, plus the event watcher that draws during an OS-modal resize drag.")
    Component(rsError, "Error guard", "native-src/dziri-engine/src/error.rs", "`catch_unwind` at every entry point; a panic becomes a status, never a crash.")
  }

  Rel(rsEngine, rsLayout, "solve")
  Rel(rsLayout, rsText, "measure")
  Rel(rsEngine, rsPaint, "draw")
  Rel(rsPaint, rsText, "paragraphs")
  Rel(rsWindow, rsEngine, "events, resize")
  Rel(rsError, rsEngine, "wraps every entry point")

  UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="2")
```
