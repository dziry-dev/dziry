{
  "symbols": 24,
  "bytes": 7706,
  "bun": "1.3.14",
  "recorded": "2026-08-05",
  "why": [
    "7562 -> 7706, +144 bytes, no new exported symbol. `typeInto` learned forward delete:",
    "Delete had done nothing at all, because the engine forwarded it like any unhandled",
    "keycode and `worker.ts` matched only Backspace. The branch is small; most of the 144 is",
    "the second slice and the `erase` union replacing a `backspace` boolean.",
    "",
    "**Re-blessed rather than left to the tolerance.** The +2% allowance had absorbed this",
    "silently — 7706 against a limit of 7714 — which is the ratchet failing at its actual",
    "job: the next change of any size would have tripped on 144 bytes it did not spend, with",
    "nothing in this file naming them. A ratchet with unrecorded slack in it is a ratchet",
    "that reports the wrong culprit.",
    "",
    "The union is worth the bytes over a second boolean: `backspace` and `forwardDelete`",
    "together can express a state no keyboard produces, and the caller would have had to",
    "decide which wins.",
    "",
    "Nothing per frame changed, and nothing about the caret: its index, blink and every",
    "movement key are still engine state (`caret.rs`), so an arrow costs no runtime bytes."
  ],
  "inventory": [
    "bindings.ts:Dirty",
    "bindings.ts:MAX_SLOT_CHARS",
    "bindings.ts:applyTextBindings",
    "bindings.ts:dispatch",
    "bindings.ts:handlerFor",
    "bindings.ts:subscribeBindings",
    "bindings.ts:typeInto",
    "list-runtime.ts:dispatchItem",
    "list-runtime.ts:readPath",
    "list-runtime.ts:subscribeLists",
    "list-runtime.ts:updateList",
    "list-runtime.ts:updateLists",
    "patches.ts:applyStylePatches",
    "patches.ts:patchesAffectLayout",
    "patches.ts:subscribeStylePatches",
    "signal.ts:$",
    "signal.ts:$m",
    "signal.ts:batch",
    "signal.ts:collectReads",
    "signal.ts:computed",
    "signal.ts:isSignal",
    "signal.ts:setCompiling",
    "signal.ts:setListBuilder",
    "signal.ts:signal"
  ]
}
