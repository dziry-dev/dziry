{
  "name": "{{name}}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.4.0"
  },
  "scripts": {
    "dev": "dziry dev",
    "build": "dziry build",
    "compile": "dziry compile",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "dziry": "{{dziry}}"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "tailwindcss": "^4.3.3",
    "typescript": "^5"
  }
}
